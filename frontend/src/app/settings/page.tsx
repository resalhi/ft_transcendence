import React, { useEffect } from "react";
import {UserEditForm} from "@/components";
const Settings = () => {

  return (
    <main>
      <UserEditForm
      />
    </main>
  );
};

export default Settings;
