// WaitingStage.tsx
import React from 'react';

const WaitStage: React.FC = () => {
  return (
    <div className="waiting-stage">
      <p>Waiting for the other player to join...</p>
    </div>
  );
};

export default WaitStage;
